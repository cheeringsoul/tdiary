version: '3.7'
services:
  webapp:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - DB_USER=root
      - DB_PASSWORD=server
      - DB_HOST=192.168.77.79
      - DB_PORT=3306
      - DB_NAME=diary
      - SECRET_KEY=dksS7^Q1~)%(;m
    volumes:
      - /Users/ymy/work/diary_image:/app/files
      - .:/app/diary
    links:
      - redis
    command: ["gunicorn", "--workers=4", "-b", "0.0.0.0:8888", "--reload", "app:application"]

  redis:
    image: redis:6.0.1-alpine

  nginx:
    image:
      nginx:alpine
    ports:
      - "80:80"
    volumes:
      - /Users/ymy/work/diary_image:/images_data
      - ./nginx:/etc/nginx/conf.d
      - ./static:/static_files
    links:
      - webapp
